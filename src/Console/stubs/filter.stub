<?php

namespace {{ namespace }};

use Closure;
use Illuminate\Database\Eloquent\Builder;

class {{ class }}
{
    public function handle(Builder $query, Closure $next): Builder
    {
        return $next($query);
    }
}